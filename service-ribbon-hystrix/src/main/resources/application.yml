server:
  port: 8766

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/


spring:
  application:
    name: service-ribbon-hystrix

service:
  client:
    name: service-hi